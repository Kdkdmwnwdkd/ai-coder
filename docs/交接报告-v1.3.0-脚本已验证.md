# AI 编程助手 · 项目交接报告（v1.3.0 已完成脚本 + Roadmap 全版）

> **用途**：下一个话题**直接把本文件拖进新对话**，说一句「按 『⚡ 第 9 节交接 SOP』+ 『第 6 节 TODO-1~5』顺序做完，最后跑附录 A 的一键出包脚本出 v1.3.0 APK，给 4 条 gh-proxy + 1 条 litterbox 国内 72h 直链」→ 100% 无缝接手，不需要重新看聊天记录。
>
> **v1.3.0 一键出包脚本位置**：仓库根目录 `scripts/release-v1.3.0.sh`（已 dry-run 验证语法全通过）
>
> **最后更新时间**：2026-09-01 18:10（✅ 脚本写完并通过语法检查 / DRY-RUN；✅ 当前进度已确认；✅ 未来 Roadmap 按用户最终决策重排）

---

## 1. 项目基本信息

| 项 | 值 |
|---|---|
| 仓库 | `https://github.com/Kdkdmwnwdkd/ai-coder` |
| 本地工作目录 | `/workspace/xuedi-coder/` |
| Git Token | `⚠️ 此处填【你的 GitHub PAT】（权限选 repo + workflow，ghp_ 开头）⚠️ --- 【绝对不要把真实 token 硬编码进 md/脚本/代码里】运行出包脚本时用命令：GITHUB_TOKEN="你的ghp_xxx" bash scripts/release-v1.3.0.sh`（用户名 `Kdkdmwnwdkd`）|
| 包名 / 架构 | `com.xuedi.coder` / **arm64-v8a only**（魅族 20 骁龙 8 Gen2）|
| 模型（用户已导入）| `Qwen2.5-3B-Instruct-Q4_K_M.gguf`（2.1 GB / 2007 MB）|
| 构建方式 | GitHub Actions `.github/workflows/build.yml`，tag 推送后 15~20 分钟出 Debug APK + Release APK |
| 安装包签名 | Debug + Release 都用固定 keystore，**版本覆盖安装不需要卸载旧版**（v1.2.0+ 已解决）|

---

## 2. 版本时间线（v1.0.0 → v1.2.9）

| 版本 | code | 核心变更 | Release 链接 |
|---|---|---|---|
| v1.0.0 | 1 | 首个可用版本：底部 4 Tab + Room 聊天持久化 + Mock 推理 | — |
| v1.1.0 | 2 | JNI 接入 llama.cpp + arm64 CMake 交叉编译 + 前台推理保活服务 | — |
| v1.2.0/v1.2.1 | 3~4 | 多话题框架（Drawer 对话列表 + new/rename/delete）；ModelManager 统一 switchAndLoadModel | — |
| v1.2.2 | 5 | 修多话题 ANR（touchActive ↔ observeAll 死循环）| — |
| v1.2.3 | 6 | ctx=0 不再静默 fallback Mock，聊天页直接返回 Error + 排障 | — |
| v1.2.4 | 7 | C++ 层修 `llama_tokenize` 返回负值（abs(need)），解决 -1214 错误 | https://github.com/Kdkdmwnwdkd/ai-coder/releases/tag/v1.2.4 |
| v1.2.5 | 8 | UI 正式化：设置页分组卡片 + 内存状态条 + 🔄 重新加载按钮；关于页 5 卡片；预热 Toast | (构建挂，重新打包在 v1.2.9 里) |
| v1.2.9 | 11 (最新) | 修编译错 clip import；collectLatest→collect；ANR 双保险 Default Dispatcher；闪退保险 5w/2w 截断 | https://github.com/Kdkdmwnwdkd/ai-coder/releases/tag/v1.2.9 |

**v1.2.9 国内直链（备用）**：
```
litter.catbox.moe 直链（72h）：https://litter.catbox.moe/oy5mzx.apk  （MD5 7232b909ba88558ea718c662ecc1752f，14.6MB）
ghproxy 加速：https://ghproxy.com/https://github.com/Kdkdmwnwdkd/ai-coder/releases/download/v1.2.9/AI.-release-95e3459-arm64-v8a.apk
```

---

## 3. 已解决的 6 个关键 Bug（根因 + 修复位置）

| 编号 | 问题现象 | 根因 | 修复位置 |
|---|---|---|---|
| B1 | 「不管哪个模型都出固定 Mock 回应」| `LlamaJniEngine.ctx==0` 时静默 fallback MockLlmEngine | [LlamaJniEngine.kt L160-198](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/model/LlamaJniEngine.kt#L160-L198) |
| B2 | 聊天页报 `llama_tokenize 失败 -1214` | `llama_tokenize(buffer=null)` 返回负值表示需要 token 数，不是错误，代码取绝对值分配 buffer | `app/src/main/cpp/llama_jni.cpp`（`llama_tokenize` 估算分支）|
| B3 | 加了多话题框架后发送消息直接 ANR 无响应（v1.2.1）| `sendMessage → touchActive → topicDao.observeAll emit → switchTopicInternal 重刷 DB 消息` 死循环 | [ChatViewModel.kt L66-93](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/vm/ChatViewModel.kt#L66-L93) |
| B4 | 设置页显示「✓ 已设为当前」但聊天报 ctx=0（用户截图核心问题）| App.kt 预热直接 `eng.loadModel()` 失败静默，没走 switchAndLoadModel 统一诊断 | [App.kt L76-114 预热段](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/App.kt#L76-L114) |
| B5 | v1.2.6 第一次真加载成功后立刻 ANR | `callbackFlow.launch {}` 没切 Dispatcher，阻塞式 nativeChat 跑主线程；StateFlow collectLatest 在 JNI 30ms 节奏下丢 token | [LlamaJniEngine.kt L210](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/model/LlamaJniEngine.kt#L210)（强制 Default）+ [ChatViewModel.kt L186](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/vm/ChatViewModel.kt#L186)（顶层 Default）+ 把 `collectLatest` 改成 `collect`（L229）|
| B6 | Debug/Release APK 不能覆盖安装（v1.1.0 老问题）| applicationIdSuffix + 每次签名不同；统一 fixedDebug keystore + release fallback | [app/build.gradle.kts L133-152](file:///workspace/xuedi-coder/app/build.gradle.kts#L133-L152) |

---

## 4. v1.2.9 用户最新反馈的 4 条问题（根因已定位，下一个话题必修）

**用户截图（2026-09-01 17:23 上传）现象总结**：
1. 🔴 顶部占一整块的「AI 编程助手」大字 + 拼音注音仍在（外层 Scaffold TopBar 没删）
2. 🔴 聊天气泡左右都有圆形头像灰块（AI/我）= 只删了发送者文字，视觉占位还保留；用户要 TRAE 风格极简（零头像零名字）
3. 🔴 「一直转、不回消息、空气泡」：3B 模型第一次 prefill 需要 20~60s，UI 没有进度状态条 + 没有取消按钮，用户不知道在干嘛，以为卡死；偶尔还闪退
4. 🔴 连点两次发送 → 同时起两个 nativeChat 协程 → 瞬间 OOM → 闪退；nativeChat 期间 ctx 被并发 release 会 JNI 崩溃

**4 条根因的代码位置（= 下一个话题要改的切入点）**：
| 问题 | 根因代码位置 |
|---|---|
| 顶部 AI 编程助手大字 | [AppNavHost.kt 旧 L64-72](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/ui/screen/AppNavHost.kt#L60-L63) — ⚠️ **本话题已修复**（Scaffold.topBar 已置空），状态：已落在磁盘，需要 commit。|
| 聊天气泡头像没删完 | [ChatPage.kt L238-346](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/ui/screen/ChatPage.kt#L238-L346) — 旧代码里 Row 里还有 40dp AI Box / 40dp 我 Box。|
| 一直转没进度无取消 | **View 侧缺状态流**：ChatViewModel 当前只有 `isTyping: Boolean`，要新增 3 条 StateFlow（见下一节 TODO）；ChatPage 输入框上方缺 Card 状态条。|
| 闪退（互斥 + 超时 + 安全）| [ChatViewModel.sendMesssage](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/vm/ChatViewModel.kt#L177-L304) 无互斥 Job；[LlamaJniEngine.chatFlow](file:///workspace/xuedi-coder/app/src/main/java/com/xuedi/coder/model/LlamaJniEngine.kt#L200-L243) 首 token 无 timeout；C++ 回调无并发 ctx 校验。|

---

## 5. 当前工作区代码状态（= 本话题已改但没推送的内容，下一个话题接手时的起点）

**⚠️ 重要：这些文件已经在磁盘上改动（还没 git commit / push），下一个话题接手时先读一下这些文件的最新内容，不要基于老版本重写！**

| 文件 | 改动内容 | 状态 |
|---|---|---|
| `ui/screen/AppNavHost.kt` | ① 去掉 `TopAppBar` / `TopAppBarDefaults` / `ExperimentalMaterial3Api` import；② 去掉 `@OptIn(ExperimentalMaterial3Api::class)`；③ `Scaffold.topBar = {}` 置空 + 写了中文注释说明干掉 AI 编程助手大字顶栏 | ✅ **已完成**（编译 OK，可直接 commit）|
| `ui/screen/ChatPage.kt` | ① 去掉 `CircleShape` / `draw.clip` import；② 新增 `Icons.Outlined.Close` import；③ `collectAsStateWithLifecycle` 移到 androidx.compose.runtime 包下面；④ ChatPage 顶部新增 3 个状态流变量：`infStatus` / `tokenCount` / `elapsedSec` 对应 ChatViewModel 里要加的 StateFlow | ⚠️ **半成品**：接下来必须：(a) ChatViewModel 加 3 个 StateFlow + 互斥 Job；(b) ChatPage 消息行的头像全部删掉；(c) ChatPage 输入框上方加状态条 + ✕ 取消按钮 |
| `vm/ChatViewModel.kt` | **未改** | ❌ 需要加 5 个东西（见 TODO-4）|
| `model/LlamaJniEngine.kt` | **未改** | ❌ 需要加首 token 15s 超时 + ctx 并发校验（TODO-4）|

---

## 6. 下一话题必做 TODO（按顺序执行，共 5 步 + 1 步出包）

### ✅ TODO-1 UI-2：聊天气泡彻底删除头像（改 ChatPage.kt 消息行 L238-346）
**目标**：用户气泡 = 右对齐淡蓝 Surface，AI 气泡 = 左对齐白 Surface，**完全没有圆形头像 Box / 名字文字 / 左右占位 Spacer**，就 TRAE 聊天那样干净。

**具体改法**：
- 删除 L240-346 整个 Row 里的：
  - `Box(40dp CircleShape + 文字 AI / 我)` 两个全部 delete
  - 左右两侧的 `Spacer(weight = 1f / 0.4f / 8dp width)` 都 delete
- 改成：
  ```kotlin
  // isUser = 用户气泡
  if (isUser) {
      Spacer(Modifier.weight(1f))
      // 气泡 Surface：RoundedCornerShape 统一 18dp，用户：primary alpha=0.14f
  } else {
      // 气泡 Surface：RoundedCornerShape 统一 18dp，AI：surface alpha=0.94f
      Spacer(Modifier.weight(1f))  // 不贴右
  }
  ```
- ⚠️ 圆角不用搞「靠头像那一侧小」（因为头像已经删了），统一 18dp 圆角就行，纯极简。
- 气泡 maxWidth：`fillMaxWidth(0.88f)` 或 `widthIn(max=320.dp)`。

### ✅ TODO-2 UX-3：输入框上方加状态条（改 ChatPage.kt，L380 输入框 Row 的上方插入一个 42dp Card）
**目标**：推理时用户能看到「已用 X 秒 / 已生成 Y 字 / 失败原因」，随时可以 ✕ 取消，不会再以为"一直转卡死"。

**状态条逻辑**：
```
infStatus = Idle → 状态条完全隐藏
infStatus = Preparing → 灰底白字：「正在准备推理…00:0X」+ 小 Progress（转圈）+ 右 ✕ IconButton（onClick=vm.cancelInference()）
infStatus = Running   → 蓝底白字：「AI 正在回复…00:12 · 已生成 ${tokenCount} 字」+ 右 ✕ 取消
infStatus = Failed(msg) → 红底白字：「推理失败：${msg.take(80)}」+ 右 ✕ 关闭
infStatus = Timeout    → 红底：「启动超时(15s)：建议减少场景开关数量或重启手机释放内存后重试」+ 右 ✕
```
- 状态条高度 42dp，padding horizontal=14dp，圆角 14dp。

### ✅ TODO-3：PluginsPage / SettingsPage / AboutPage 顶部标题行缩小（删掉所有 >20sp 的超大号大标题）
这 3 个页面用户截图的「场景·插件开关」「本地模型管理」「应用信息」第一行字号是 18sp。用户要的是正式感 → 统一缩小到 15sp SemiBold + 主色调，和 SettingsPage 里的 SectionHeader 风格一致（不用再加全局 TopBar）。

### ✅ TODO-4 PERF：推理互斥 + 超时 + 安全（核心防闪退，改 ChatViewModel + LlamaJniEngine）
#### 4a. ChatViewModel 新增（文件顶部 StateFlows 区域加）：
```kotlin
enum class InfStatus { Idle, Preparing, Running, Failed, Timeout }
private val _infStatus = MutableStateFlow(InfStatus.Idle)
val inferenceStatus = _infStatus.asStateFlow()
private val _currentTokenCount = MutableStateFlow(0)
val currentTokenCount = _currentTokenCount.asStateFlow()
private val _infElapsedMs = MutableStateFlow(0L)
val inferenceElapsedSec = _infElapsedMs.map { it/1000 }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), 0)
// 防止连点两次同时跑两个 nativeChat → OOM 闪退
private val inferenceMutex = Mutex()    // 加 import：kotlinx.coroutines.sync.Mutex / withLock
private var inferenceJob: Job? = null
private var failMsg: String? = null
private val _failMsgFlow = MutableStateFlow<String?>(null)
val failMsgFlow: StateFlow<String?> = _failMsgFlow.asStateFlow()
```
#### 4b. sendMessage 开头加 `inferenceMutex.withLock` → 如果 inferenceJob != null && isActive → 先 cancelInference() 再跑新的，保证同一时间只有一个 nativeChat。
#### 4c. _infStatus = Preparing 时开 `_infElapsedMs` ticker（每 500ms 加 500ms，配合进度条）。
#### 4d. 收到第一个 ChatChunk.Token → `_infStatus.value = Running, _currentTokenCount += piece.length`，Done/Error → Idle。
#### 4e. LlamaJniEngine.chatFlow 首 token 15s 超时：在 callbackFlow 内部 job 外面包一个 `withTimeoutOrNull(15_000)` 等 first emit，如果超时 `trySend(ChatChunk.Error("首 token 超时(15s)…"))` + `nativeChatCancel(curCtx)` + close 流。
#### 4f. LlamaJniEngine onToken/onDone 回调前判 `if (ctx != curCtx) return`（ctx 被并发 release 时 JNI 不调旧指针，避免 SIGSEGV 闪退）。

### ✅ TODO-5 BUILD：v1.3.0 打 tag 推送 + 下载链接
**version bump**：`app/build.gradle.kts` L27-28 → versionCode=12, versionName="2.2.0-M11-TRAE-UI"
**commit message**（别加换行、括号、emoji，避免 zsh 解析错误）：`v1.3.0 TRAE极简UI + 全局顶栏删除 + 推理状态条 + 超时互斥防闪退`
**git tag & push**：
```
git add -A && git commit -m "v1.3.0 TRAE极简UI + 全局顶栏删除 + 推理状态条 + 超时互斥防闪退"
git tag v1.3.0
git push origin main v1.3.0
```

**出包完（Release 页 APK Assets 已挂）后，给用户 5 条链接**：
```
ghproxy：https://ghproxy.com/https://github.com/Kdkdmwnwdkd/ai-coder/releases/download/v1.3.0/AI.-release-XXXXXXX-arm64-v8a.apk
mirror.ghproxy：https://mirror.ghproxy.com/https://github.com/Kdkdmwnwdkd/ai-coder/releases/download/v1.3.0/AI.-release-XXXXXXX-arm64-v8a.apk
gh.llkk.cc：https://gh.llkk.cc/https://github.com/Kdkdmwnwdkd/ai-coder/releases/download/v1.3.0/AI.-release-XXXXXXX-arm64-v8a.apk
99988866：https://gh.api.99988866.xyz/https://github.com/Kdkdmwnwdkd/ai-coder/releases/download/v1.3.0/AI.-release-XXXXXXX-arm64-v8a.apk
国内直链（litterbox 72h）：用 curl 跑下面两行，返回 URL 给用户：
   curl -sSL -F "time=72h" -F "reqtype=fileupload" -F "fileToUpload=@本地APK路径" https://litterbox.catbox.moe/resources/internals/api.php
```

---

## 7. 用户验证清单（装完 v1.3.0 必须让用户拍 4 张截图）

1. **聊天页首屏截图** → 验收：
   - ✅ **顶部没有「AI 编程助手」大字**（只有 ChatPage 内部小 Row：☰ + 话题名 + ➕）
   - ✅ **没有拼音注音在字头顶**（这是魅族 Flyme 的系统开关，非代码问题，需要提醒用户关：设置 → 无障碍 → 汉字显示拼音注音）
2. **发消息后 30 秒截图** → 验收：
   - ✅ 气泡左右都没有圆形灰头像，也没有「我 / AI 编程助手」文字名
   - ✅ 输入框上方状态条显示「AI 正在回复…00:XX · 已生成 N 字」+ 有 ✕ 取消按钮
3. **长回复 60 秒截图** → 验收：不会闪退（互斥 + 截断双保险生效）
4. **设置页截图** → 验收：模型行内存状态条仍绿 ✓（ctx 非 0），设置/场景/关于 顶部没有 AI 编程助手大标题

---

## 8. 常见坑提示（下一个 Agent 接手时别踩）

1. **zsh commit message 解析失败**：commit message 里别加 `()`、别加 `\n\n` 多行，一句话纯中文 + 加减号，最简单的格式不会挂。
2. **clip import 包名**：Compose 的 `Modifier.clip(CircleShape)` 正确 import 是 `androidx.compose.ui.draw.clip`，**不是** `foundation.clip`（v1.2.8 就是因为这个编译挂了 2 次）。
3. **kotlin 编译报错 Unresolved reference: inferenceStatus**：ChatViewModel 加了 StateFlow 之后一定要在 ChatPage.kt 顶部声明 `by vm.inferenceStatus.collectAsStateWithLifecycle()`，漏写就是编译错。
4. **GitHub 国内 404 / 卡**：所有 gh-proxy 前缀格式统一 `https://ghproxy.com/` + 完整原始 URL，中间不能少 `https://`。如果 gh-proxy 全封，**一定用 litterbox 上传**（沙箱这边下载 GitHub APK 到 `/tmp/xxx.apk`，然后 curl 到 litterbox 拿短链，这个方案在 v1.2.9 已经实测成功过）。
5. **CI 构建时间**：第一次 push tag 后因为 llama.cpp CMake 要编 C++ 大概 18~22 分钟；第二次往后 Gradle 命中缓存会快 30%。如果 25 分钟还没出 APK 先看 Actions 的 log（`compileDebugKotlin` 有没有红），不要傻傻等。

---

## 9. ⚡ 下一个对话 Agent · 前 3 步必做（交接 SOP，100% 按顺序跑）

**拿到本报告后第一时间执行，不要先乱改代码！**

| 步骤 | 命令 / 动作 | 验证点（必须 OK 才往下走）|
|---|---|---|
| **① 环境 & 脚本自检** | `cd /workspace/xuedi-coder && bash scripts/release-v1.3.0.sh --dry-run` | 输出必须有 6 个 `[阶段 1/6 → 阶段 6/6]`、结尾必须打印 4 条 gh-proxy 链接格式正确；任何报错先修（缺 git/curl/JDK<17），不要先改代码 |
| **② 补代码（按第 6 节 TODO-1~4 顺序）** | 先改 ViewModel 3 个 StateFlow + 互斥锁 → 再改 ChatPage 删头像 → 再加状态条 → 最后加 LlamaJniEngine 超时 | 每改完一个文件先 `bash scripts/gradlew :app:compileDebugKotlin` 语法检查（不要直接 push 让 CI 挂了再查） |
| **③ 正式出包（去掉 --dry-run）** | `GITHUB_TOKEN="用户给的 ghp_xxx token" bash scripts/release-v1.3.0.sh` | 脚本必须跑满到第 6 阶段输出 5 条链接（4 ghproxy + 1 litterbox），**少一条都不要给用户**（litterbox 失败就手跑脚本末尾给的 3 行 curl 命令） |

> ⚠️ **安全护栏（脚本自带，不用额外操作）**：push 失败自动回滚本地 tag + 回滚脚本自己创建的 commit（不回滚用户之前的代码）+ sed 替换 versionCode 之后 grep 双重校验成功才继续，不会出现 tag 已打 code 没 bump 的翻车情况。

---

## 10. 📌 当前进度（2026-09-01 18:10，v1.3.0）

### 10.1 代码改动（已落在 `/workspace/xuedi-coder/` 磁盘，待 commit）
| 文件 | 已改内容 | 状态 | 验收 |
|---|---|---|---|
| `ui/screen/AppNavHost.kt` | ① 删 TopAppBar/ExperimentalMaterial3Api import；② 删 @OptIn 注解；③ Scaffold.topBar 置空 + 中文注释说明（彻底干掉顶部「AI 编程助手」大字顶栏）| ✅ 完成（可直接 commit）| 聊天/场景/设置/关于 4 个页面**最上面那行大字 + 拼音注音（系统开关除外）完全消失** |
| `ui/screen/ChatPage.kt` | ① 删 CircleShape / draw.clip import；② 加 Outlined.Close import；③ 顶部 StateFlow 变量声明：`infStatus / tokenCount / elapsedSec`（对应 ChatViewModel TODO 里要加的 3 个流）| ⚠️ 半成品 | 还需要 TODO-1（删头像 Box）+ TODO-2（加 42dp 状态条 + ✕ 取消）|
| `vm/ChatViewModel.kt` | **未改** | ❌ TODO 要加：3 个 StateFlow（InfStatus 枚举 + tokenCount + elapsedSec）、inferenceMutex 互斥锁 + inferenceJob 连点取消、sendMessage 包一层 withLock、状态流转（Preparing → Running → Failed/Timeout）|
| `model/LlamaJniEngine.kt` | **未改** | ❌ TODO 要加：首 token 15s withTimeoutOrNull 超时、onToken/onDone 前 `if (ctx != curCtx) return` 并发安全校验 |
| `app/build.gradle.kts` | **未改**（脚本会在 Phase2 自动 bump versionCode=12 / versionName=2.2.0-M11-TRAE-UI）| ✅ 脚本搞定 | 不用手动改，脚本 sed + grep 双重校验 |

### 10.2 出包脚本（✅ 已完成 + 已验证）
| 项 | 值 |
|---|---|
| 脚本路径 | `scripts/release-v1.3.0.sh`（chmod +x 已赋予执行权限）|
| 语法检查 | `bash -n` 全通过 ✅ |
| dry-run 验证 | 6 阶段输出正常，加速链接格式正确 ✅ |
| 核心能力 | 6 阶段全自动：环境检查 → version bump → commit/tag 失败自动回滚 → https+token push → 40 分钟轮询 Release → 输出 4 ghproxy + 1 litterbox 直链 |
| 回滚护栏 | trap ERR/INT + message 匹配回滚（不会误删用户之前的 commit）、version bump grep 双重校验、sed 非空校验防 `versionCode = ` 空值 |

---

## 11. 🚀 未来功能 Roadmap v2.0（按用户最终决策 · 优先级排序）

> 以下功能**全是"用到再做"**，v1.3.0 聊天能发"你好"收到回复之前，一个都不要开工，避免基础盘不稳。优先级：P1 > P2 > P3。

| 版本 | 功能 | 一句话说明（用户确认要的才放进来）| 优先级 | 依赖 |
|---|---|---|---|---|
| v1.3.0 | ✅ **基础聊天稳定性**（互斥锁 + 状态条 + 超时 + 极简 UI）| 发消息不闪退、不永远转，用户能真聊上 | 必做 | 第 6 节 TODO 1~5 |
| v1.3.1 | 🌐 **联网搜索模块（RAG 基础版）**| DuckDuckGo HTML + 3 SearXNG 轮询，搜最新新闻/热搜/版本，回答附 3 条来源链接 + 加缓存 + 超时兜底，不用注册 key | ⭐⭐⭐⭐⭐ **P1** | v1.3.0 聊通 |
| **v1.3.2** | 🐙 **GitHub 能力接入（用户最要的那个）**| OAuth 授权登录（scope 最小：repo/workflow/read:user，加密 token 存 EncryptedDataStore）；AI 能直接读/改仓库代码、自动打 tag 触发 Actions 出 APK、查 Release、开 issue、搜你自己 star 过的项目；GitHub API 加 3 层 gh-proxy 加速代理 + 测速选最快 | ⭐⭐⭐⭐⭐ **P1 最高** | v1.3.1 联网基础 |
| v1.4.0 | 🏆 **真跑代码沙箱**（代码执行插件）| Chaquopy 跑 Python + J2V8 跑 JS，沙箱临时目录隔离，代码块右上角 `▶️ 运行` → 底下冒 100dp 小终端实时输出 stdout/stderr，不 root 能用 | ⭐⭐⭐⭐ **P0（实用）** | v1.3.2 之后 |
| v1.5.0 | 🧠 **短/长期记忆模块**（用户之前点过名要的）| 短期：超 2048 token 自动摘要压缩塞上下文不失忆；长期：每 10 条消息存一条 BM25 向量到 Room 记忆库，新话题自动带上「用户正在做 xx 项目」的背景 | ⭐⭐⭐⭐ **P1** | v1.4.0 |
| v1.5.1 | 🧩 **MCP 插件框架基础版**（复用 TRAE 生态）| 独立 `files/plugins/` 目录，插件 3 状态：启用/禁用/删除；支持 TRAE MCP 格式 JSON 导入，不用改主程序；**先接 2 个用户要的，其他等用到再加** | ⭐⭐⭐ **P2** | v1.5.0 |
| v1.5.2 | 🖼️ **文生图插件（Seedream）**| 插件系统第一个插件，接 TRAE 官方 Seedream 文生图云端 API（不本地跑 SD，手机跑不动），生成的图直接插入聊天气泡可保存相册 | ⭐⭐ **P2（用到再做）** | 插件框架 + 用户说"要做" |
| v1.5.3 | 🎬 **文生视频插件（Seedance）**| 同上图插件，接 TRAE 官方 Seedance 文生 5 秒 720p MP4，聊天记录里直接点播放 | ⭐⭐ **P2（用到再做）** | 插件框架 + 用户说"要做" |
| v1.6.0+ | 🔌 **其他插件**（天气/飞书/GitHub 更复杂的操作等）| 全是"用到再说"，TRAE MCP 生态有现成的直接 import JSON，不想用了删 `files/plugins/` 下对应文件，不影响主程序聊天 | ⭐ **P3（按需）** | 用户明确说"我要做 XX 插件" 才开工 |

---

## 📎 附录 A：v1.3.0 一键出包完整脚本（复制即跑 · 和 `scripts/release-v1.3.0.sh` 内容完全一致）

> 💡 万一仓库里 `scripts/` 目录丢了，直接把下面代码块**复制到终端保存成 `release-v1.3.0.sh` 再 `chmod +x` 就能跑**，效果和脚本文件一模一样。

```bash
#!/usr/bin/env bash
# =============================================================================
#  🚀 AI 编程助手 v1.3.0 一键出包脚本（复制即跑 · 含安全护栏 · dry-run 模式）
# =============================================================================
#  用法（3 选 1）：
#    1. 直接跑（交互填 token）：  bash scripts/release-v1.3.0.sh
#    2. 环境变量塞 token：         GITHUB_TOKEN="ghp_xxx" bash scripts/release-v1.3.0.sh
#    3. DRY-RUN 只看步骤不操作：   bash scripts/release-v1.3.0.sh --dry-run
# =============================================================================
set -euo pipefail

DRY_RUN=false
if [[ "${1:-}" == "--dry-run" ]]; then
  DRY_RUN=true
  echo -e "\033[1;33m[DRY-RUN] 只打印步骤，不执行任何写操作（commit/push/tag）\033[0m"
fi

GITHUB_USER="Kdkdmwnwdkd"
GITHUB_REPO="ai-coder"
VERSION_TAG="v1.3.0"
VERSION_CODE=12
VERSION_NAME="2.2.0-M11-TRAE-UI"
BUILD_FILE="app/build.gradle.kts"
COMMIT_MSG="v1.3.0 TRAE-minimal-UI + status-bar + timeout + mutex-anti-OOM"

if [[ -z "${GITHUB_TOKEN:-}" ]]; then
  if $DRY_RUN; then
    echo "[DRY-RUN] 跳过 GITHUB_TOKEN 检查"
    GITHUB_TOKEN="DRY-RUN-TOKEN-PLACEHOLDER"
  else
    read -r -p "👉 请输入 GitHub PAT（repo + workflow 权限）：" GITHUB_TOKEN
    if [[ -z "$GITHUB_TOKEN" ]]; then
      echo -e "\033[1;31m❌ 必须提供 GITHUB_TOKEN 才能 push 触发 CI，退出。\033[0m" >&2
      exit 1
    fi
  fi
fi

echo -e "\033[1;36m=== 🛠️  阶段 1/6：环境检查 ===\033[0m"
require_cmd() {
  if ! command -v "$1" &>/dev/null; then
    echo -e "\033[1;31m❌ 缺少命令：$1，请先安装再运行脚本\033[0m" >&2
    exit 1
  fi
}
require_cmd git; require_cmd curl
if [[ ! -d ".git" ]] || [[ ! -f "$BUILD_FILE" ]]; then
  echo -e "\033[1;31m❌ 请在仓库根目录（含 .git 和 $BUILD_FILE）执行本脚本，当前目录：$(pwd)\033[0m" >&2
  exit 1
fi
JAVA_VER=$(java -version 2>&1 | head -1 | grep -oE '"[0-9]+' | tr -d '"' || echo "0")
if [[ "$JAVA_VER" -lt 17 ]] && ! $DRY_RUN; then
  echo -e "\033[1;33m⚠️  当前 JDK 版本=$JAVA_VER，推荐 JDK 17+（AGP 8.x 要求），你可以继续但 build.gradle 版本号 bump 可能跳过\033[0m"
fi
echo "✅ 仓库目录正确：$(pwd) · 基础环境 OK（git/curl/JDK$JAVA_VER）"

echo -e "\n\033[1;36m=== 🔢  阶段 2/6：版本号 bump（code=$VERSION_CODE, name=$VERSION_NAME）===\033[0m"
bump_version() {
  [[ -z "$VERSION_CODE" || -z "$VERSION_NAME" ]] && { echo "❌ versionCode/Name 空值，拒绝替换" >&2; exit 1; }
  sed -i \
    -e "s/versionCode = [0-9]*/versionCode = $VERSION_CODE/" \
    -e "s/versionName = \".*\"/versionName = \"$VERSION_NAME\"/" \
    "$BUILD_FILE"
  grep -q "versionCode = $VERSION_CODE" "$BUILD_FILE" && grep -q "versionName = \"$VERSION_NAME\"" "$BUILD_FILE"
}
if $DRY_RUN; then
  echo "[DRY-RUN] 将执行 sed 替换 $BUILD_FILE 中的 versionCode/versionName"
else
  bump_version && echo "✅ $BUILD_FILE 版本号替换成功（code=$VERSION_CODE, name=$VERSION_NAME）"
fi

echo -e "\n\033[1;36m=== 📝  阶段 3/6：Git Commit + 打 Tag $VERSION_TAG（失败自动回滚）===\033[0m"
if $DRY_RUN; then
  echo "[DRY-RUN] 将执行：git add -A && git commit -m \"$COMMIT_MSG\" && git tag $VERSION_TAG"
else
  git add -A
  if git commit -m "$COMMIT_MSG" --allow-empty; then
    echo "✅ git commit 成功"
  else
    echo -e "\033[1;33m⚠️  git commit 无改动（可能上一版已提交），继续尝试打 Tag\033[0m"
  fi
  if git rev-parse "$VERSION_TAG" &>/dev/null; then
    echo "⚠️  检测到本地已存在 $VERSION_TAG，自动删除旧 tag 后重建"
    git tag -d "$VERSION_TAG"
  fi
  git tag "$VERSION_TAG" && echo "✅ git tag $VERSION_TAG 创建成功"
fi

rollback_on_fail() {
  echo -e "\n\033[1;31m❌ Push 失败，触发安全回滚：删本地 tag + 回滚 commit\033[0m"
  git tag -d "$VERSION_TAG" 2>/dev/null || true
  LAST_MSG=$(git log -1 --pretty=%B)
  if [[ "$LAST_MSG" == "$COMMIT_MSG" ]]; then
    echo "🔙 回滚 commit：git reset --mixed HEAD~1（保留你工作区改动，不丢代码）"
    git reset --mixed HEAD~1
  else
    echo "ℹ️  最近一条 commit 不是本脚本创建的，跳过 commit 回滚（只删 tag）"
  fi
  exit 1
}
trap rollback_on_fail ERR INT

echo -e "\n\033[1;36m=== 🚀  阶段 4/6：Push 代码 + Tag 触发 GitHub Actions CI 构建 ===\033[0m"
GIT_REMOTE_URL="https://$GITHUB_USER:$GITHUB_TOKEN@github.com/$GITHUB_USER/$GITHUB_REPO.git"
if $DRY_RUN; then
  echo "[DRY-RUN] 将执行：git push origin main && git push origin $VERSION_TAG（走 https + token 认证，不走 ssh）"
else
  echo "🌐 Push 到远程仓库（https 方式，token 安全认证不输出到 log）"
  git push "$GIT_REMOTE_URL" main
  git push "$GIT_REMOTE_URL" "$VERSION_TAG"
  trap - ERR INT
  echo "✅ Push 成功！GitHub Actions 已触发，接下来自动轮询 Release 出包状态"
fi

echo -e "\n\033[1;36m=== ⏳  阶段 5/6：轮询 Release Assets（每 60s 查一次，最多 240 次 = 40 分钟）===\033[0m"
RELEASE_API="https://api.github.com/repos/$GITHUB_USER/$GITHUB_REPO/releases/tags/$VERSION_TAG"
APK_ASSET_NAME_PATTERN="arm64-v8a.apk"
MAX_WAIT=240
WAITED=0
APK_DOWNLOAD_URL=""
if $DRY_RUN; then
  echo "[DRY-RUN] 将循环调用 GitHub API 查 Release 页，等 APK asset 挂好后打印加速链接"
else
  while [[ $WAITED -lt $MAX_WAIT ]]; do
    RESP=$(curl -sSL -u "$GITHUB_USER:$GITHUB_TOKEN" "$RELEASE_API" || echo "{}")
    APK_URL=$(echo "$RESP" | grep -oE '"browser_download_url": *"[^"]*'"$APK_ASSET_NAME_PATTERN"'"' | head -1 | cut -d'"' -f4 || true)
    if [[ -n "$APK_URL" ]]; then
      APK_DOWNLOAD_URL="$APK_URL"
      echo -e "\033[1;32m✅ APK Assets 挂好了！耗时 $((WAITED * 60 / 60)) 分钟\033[0m"
      break
    fi
    STATE=$(echo "$RESP" | grep -oE '"message": *"[^"]*"' | head -1 | cut -d'"' -f4 || echo "Release 未出现在 API（CI 还在构建）")
    echo -e "⏳ 已等 $((WAITED * 60 / 60)) 分 / 40 分，当前状态：$STATE"
    WAITED=$((WAITED + 1))
    sleep 60
  done
  if [[ -z "$APK_DOWNLOAD_URL" ]]; then
    echo -e "\033[1;31m❌ 超过 40 分钟 APK 还没挂好，请手动打开 GitHub Actions 查构建日志：https://github.com/$GITHUB_USER/$GITHUB_REPO/actions\033[0m" >&2
    exit 1
  fi
fi

echo -e "\n\033[1;36m=== 📦  阶段 6/6：生成下载链接（4 条 gh-proxy + 国内 72h 直链）===\033[0m"
if [[ -z "$APK_DOWNLOAD_URL" ]] && ! $DRY_RUN; then
  echo "❌ 没有 APK URL，跳过生成链接" >&2
  exit 1
fi
$DRY_RUN && APK_DOWNLOAD_URL="https://github.com/$GITHUB_USER/$GITHUB_REPO/releases/download/$VERSION_TAG/AI.-release-XXXXXXXX-arm64-v8a.apk"

echo -e "\n\033[1;32m🎉 v1.3.0 APK 下载链接（按加速成功率排序）：\033[0m"
echo "1️⃣  gh-proxy 主站：   https://ghproxy.com/$APK_DOWNLOAD_URL"
echo "2️⃣  gh-proxy 镜像1：  https://mirror.ghproxy.com/$APK_DOWNLOAD_URL"
echo "3️⃣  gh.llkk.cc：      https://gh.llkk.cc/$APK_DOWNLOAD_URL"
echo "4️⃣  99988866 CDN：    https://gh.api.99988866.xyz/$APK_DOWNLOAD_URL"

if $DRY_RUN; then
  echo -e "\n\033[1;33m[DRY-RUN] 真实出包后，运行以下命令拿 litterbox 72h 国内直链：\033[0m"
  echo "    APK_LOCAL_PATH=\"/tmp/ai-coder-$VERSION_TAG.apk\" && \\"
  echo "    curl -sSL -o \"\$APK_LOCAL_PATH\" \"https://ghproxy.com/$APK_DOWNLOAD_URL\" && \\"
  echo "    curl -sSL -F \"time=72h\" -F \"reqtype=fileupload\" -F \"fileToUpload=@\$APK_LOCAL_PATH\" https://litterbox.catbox.moe/resources/internals/api.php"
else
  echo -e "\n\033[1;34m🔗 尝试自动生成 litterbox 72h 国内直链（不需要请 Ctrl+C 跳过）...\033[0m"
  TMP_APK="/tmp/ai-coder-$VERSION_TAG-$$.apk"
  LITTER_URL=""
  if curl -sSL --max-time 600 -o "$TMP_APK" "https://ghproxy.com/$APK_DOWNLOAD_URL" && [[ -s "$TMP_APK" ]]; then
    echo "✅ APK 本地下载完成（$(du -h "$TMP_APK" | cut -f1)），上传 litterbox..."
    LITTER_URL=$(curl -sSL --max-time 120 -F "time=72h" -F "reqtype=fileupload" -F "fileToUpload=@$TMP_APK" https://litterbox.catbox.moe/resources/internals/api.php || true)
    rm -f "$TMP_APK"
  fi
  if [[ -n "$LITTER_URL" && "$LITTER_URL" == https* ]]; then
    echo -e "\n\033[1;32m🚀 国内 72h 直链（魅族 20 直接点开就能下，不用翻墙）：\033[0m"
    echo "5️⃣  litterbox 直链：    $LITTER_URL"
  else
    echo -e "\n\033[1;33mℹ️  自动上传 litterbox 失败（网络原因），请手动执行以下命令拿国内直链：\033[0m"
    echo "    APK_LOCAL_PATH=\"/tmp/ai-coder-$VERSION_TAG.apk\" && \\"
    echo "    curl -sSL -o \"\$APK_LOCAL_PATH\" \"https://ghproxy.com/$APK_DOWNLOAD_URL\" && \\"
    echo "    curl -sSL -F \"time=72h\" -F \"reqtype=fileupload\" -F \"fileToUpload=@\$APK_LOCAL_PATH\" https://litterbox.catbox.moe/resources/internals/api.php"
  fi
fi

echo -e "\n\033[1;32m=====================================================================\033[0m"
echo -e "\033[1;32m 🎉 v1.3.0 出包流程全跑完，用户验证清单看项目报告第 7 节（4 张截图）\033[0m"
echo -e "\033[1;32m=====================================================================\033[0m"
```

